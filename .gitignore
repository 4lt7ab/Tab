# Claude Code worktrees and local settings
.claude/worktrees/
.claude/settings.local.json

# OS files
.DS_Store
Thumbs.db

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Node (if any tooling is added)
node_modules/
npm-debug.log*

# Python / uv (cli/)
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/

# asdf — tool version pins are per-machine
.tool-versions

# Environment
.env
.env.*

.local/

# grimoire-core's cwd-relative SQLite fallback. The Tab runtime pins
# the DB to ~/.tab/grimoire.db; this directory only appears when
# something runs grimoire-core before the pin (e.g. an ad-hoc REPL).
.grimoire/
